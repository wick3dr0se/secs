[package]
name = "secs"
version = "0.1.0"
edition = "2021"

[dependencies]
bimap = "0.6.3"
rayon = { version = "1.10.0", optional = true }
thunderdome = "0.6.1"

[features]
multithreaded = ["rayon"]

[dev-dependencies]
macroquad = "0.4.13"
